import 'package:flutter/material.dart';

class UserBillPage extends StatelessWidget {
  const UserBillPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
