import 'package:flutter/material.dart';

class UserLabReport extends StatelessWidget {
  const UserLabReport({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
